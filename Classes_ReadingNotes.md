# Test B1


1. What is a class?
- Class is a blueprint used to build objects, similar to methods.

2. What is an object?
- object implies to the creation when using class

3. How do we identify an object?
- this.(Object)

4. What is the data inside the object called?
- An Argument

5. How did we create objects in 201?
- We used constructor functions, methods, and prototypes

6. Explain the difference between a parameter and an argument:
- Parameter helps determine what keywords your class wants to use.
- ex: tank = T-90
     class = (tank) {

    };
    Argument is the information in an object
- ex: ammunition[0] = 42;

7. What is special about a class's name?
- class' name cannot be bind to anything, allowing to have free control and to be assigned to anything

8. What does the constructor method do?
- Allows us to pass information back up to the upper branches that class cant.

9. What keyword will allow a constructor to call the constructor of a superclass?
- super()

10. What keyword creates a new instance of a class?
- class (parameter) {
    desc = green
}